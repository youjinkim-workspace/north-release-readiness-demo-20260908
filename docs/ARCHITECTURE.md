# Review workflow

GitHub checklist + live PR metadata -> evidence comparison -> human review -> new Google Sheet.

The application is a Python reference implementation of review rules. North performs a separate connector-based inspection. No production deployment is represented.
